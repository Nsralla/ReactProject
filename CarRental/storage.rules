rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Matches all files in the storage
    match /{allPaths=**} {
      // Allows read and write access to authenticated users
      allow read, write: if request.auth != null;
    }
  }
}
